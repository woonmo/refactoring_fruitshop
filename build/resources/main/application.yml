spring:
  profiles:
    active: ${ENV:dev}

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    open-in-view: false # 성능 하락 방지

server:
  port: ${SERVER_PORT:8080}